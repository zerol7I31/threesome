# 问题 3 / 问题 4 运行指南（含官方模拟器）

工作目录：`C:\Users\yangj\Desktop\B题`

```
jammers-simulator.exe          ← 官方模拟器（绿色软件，双击即可运行）
JammersSimulatorData\          ← 模拟器数据目录（务必与 exe 一起整体移动！）
├─ startup.log
├─ upload-queue.sqlite3
├─ formal-statistics-queue.sqlite3
└─ practice-statistics-queue.sqlite3
p3_arena.py                    ← 通信层：离线模拟器 MockArena + 真实模拟器客户端 HttpArena
p3_robot.py                    ← 机器狗策略 + 演练驱动
p3_mock_server.py              ← 本地 HTTP 模拟器（把 MockArena 用 HTTP 暴露出来）
logs\                          ← 程序自行记录的指令序列日志（附件要求）
```

> **重要**：`jammers-simulator.exe` 与 `JammersSimulatorData` 必须放在同一层目录。
> 要换位置就整体搬走，否则登录态/队列数据会丢。

---

## 一、三种运行方式（建议按顺序做）

### 方式 A：完全离线，只用本地 HTTP 模拟器（推荐先做）

作用：验证"机器狗程序 → HTTP → 模拟器"这条链路（JSON 编码、`request_id` 幂等、`accepted` 判定、
连接失败重试）是否正常，**不联网、不需要登录、不消耗任何正式测试机会**。

开两个终端（都在 `C:\Users\yangj\Desktop\B题`）：

```bash
# 终端 1：启动本地模拟器（默认 127.0.0.1:2026）
python p3_mock_server.py
#   可选：--port 2027 换端口；--directional 生成含定向源的案例（问题 4）；--dist boundary 指定分布

# 终端 2：运行机器狗
python p3_robot.py live --url http://127.0.0.1:2026 --team TESTTEAM --wait-enter 15
```

期望输出：

```
连接真实模拟器 http://127.0.0.1:2026 （robot_id = TESTTEAM）
正在等待接口开放（最多 15.0 秒）——请先在模拟器里点开始并等完 5 秒倒计时。
结束：清除 16 个，虚拟时间 7511.0 s
平均定位清除时间：469.4 s   （= 虚拟时间 / 清除个数）
动作统计： {'measures': 111, 'clears': 16, 'misses': 0, 'moves': 33910.0}
本机指令日志已保存：logs\live-20260911-121250.jsonl
```

> 已实测通过：本地模拟器 + live 客户端跑通全流程，清除 16/16。

### 方式 B：连官方模拟器做**演练测试**（不限次数）

```bash
# 第 1 步：双击运行 jammers-simulator.exe（必须在 2026 端口空闲时启动）
#   · 首次使用：填参赛队号 + 队员1姓名 + 队员1手机号，核对信息后设密码完成注册
#     密码 12~64 位，必须同时含字母和数字，且不能包含队号/手机号里的连续 6 位数字
#   · 模拟器会联网校验登录状态与服务器时间（相差 >60 秒无法开局）
# 第 2 步：在界面选择「问题3演练测试」→ 点击开始 → 模拟器准备数据 → 5 秒倒计时
# 第 3 步：倒计时一结束（界面提示"接口已就绪"）立即运行：
python p3_robot.py live --url http://127.0.0.1:2026 --team <你的参赛队号> --wait-enter 60
```

**时间要点**（附件原文）：25 分钟窗口开始后 **5 分钟内**成功调用 `/enter` 才能用足 20 分钟程序运行时间。
所以第 3 步要快，或者干脆**先把第 3 步运行起来**——本程序会自己等接口开放（`--wait-enter`）。

### 方式 C：正式测试（每题只有 3 次机会）

流程同方式 B，但：

- 选「问题3正式测试」/「问题4正式测试」，界面会再次确认，**启动即占用一次机会**；
- **"中止测试"也占用一次机会**，所以务必先在 A/B 里把程序调稳；
- 正式测试**不显示干扰源总数**，只能靠程序自己判定；
- 结束后从模拟器【日志列表】**导出加密日志**（**不要改文件名**）放进支撑材料。

---

## 二、日常离线调参（不碰官方模拟器）

```bash
python p3_robot.py mock    --episodes 20                  # 批量演练，看两项统计
python p3_robot.py mock    --episodes 20 --seed0 50000    # 换种子独立验证（重要！）
python p3_robot.py sweep   --episodes 5                   # 参数扫描（改动 sweep 里的 grid）
python p3_robot.py dist    --episodes 6                   # 5 种空间分布的鲁棒性
python p3_robot.py analyze --episodes 300                 # 用演练数据反推空间分布
python p3_robot.py mock    --episodes 3 --directional     # 问题 4（含定向源）
```

---

## 三、按表 1 记录结果

| 测试案例编码 | 清除干扰源个数 | 平均定位清除时间 | 程序运行时间 |
|---|---|---|---|
| 测试 1 | （模拟器界面显示） | 程序打印 | 程序打印 / 模拟器显示 |
| 测试 2 | | | |
| 测试 3 | | | |

- **测试案例编码**：模拟器每局都会显示，也在日志列表里长期可见。
- **平均定位清除时间** = 虚拟时间 ÷ 清除个数（程序已直接打印）。
- **程序运行时间** = `/enter` 成功到测试结束的现实时长（≤ 20 min）。

---

## 四、常见问题排查

| 现象 | 原因 / 处理 |
|---|---|
| `upstream connect failed / 目标计算机积极拒绝` | 模拟器没运行，或还停在 5 秒倒计时里。等倒计时结束后程序会自动重试。 |
| 端口 2026 被占用 | 我之前跑的本地模拟器若还在后台，先关掉；或在官方模拟器设置里换端口，然后 `--url` 同步改。 |
| `/enter` 返回 `accepted=false` | ①队号与登录的不一致 ②重复调用 `/enter` ③本机时间与服务器时间差 >60 秒。 |
| 网络断开 | 程序对同一动作会复用**原请求体与原 request_id** 重试，这是协议要求的幂等做法，不会重复移动/检测。 |
| 不能启动新测试 | 17:30 后禁止启动新的演练/正式测试（已开始的可以正常跑完）。 |
| 加密日志上传失败 | 只要已进入上传队列，模拟器会自动重试，不受 17:30 限制。日志 ≤ 2 MB，正常策略不会超。 |

---

## 五、时间提醒

- 北京时间 **2026-09-13 17:30** 之后不能启动新的演练测试或正式测试；
- 建议 **2026-09-13 15:30 之前**完成全部正式测试（避开网络拥堵）；
- 每题正式测试各 3 次，务必留出余量：**先在方式 A/B 把参数调稳再上正式测试**。
