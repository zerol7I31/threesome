# 问题 4 演练优化分析（case 3SBT-7YXC-ZKCY-A4XM）

> 数据源：真实模拟器本地汇总库 `JammersSimulatorData/practice-statistics-queue.sqlite3`
> （`.jlog` 行为是 AES-256-GCM 加密的，无法直接读明文；汇总成绩在 sqlite 里是明文）。
> 配套 `result.json` 给出本局构成：12 源 = 3 全向 + **9 定向**。

## 一、本局成绩（已确认）

| 指标 | 值 | 评价 |
|---|---|---|
| 干扰源总数 | 12（3 全向 + 9 定向）| — |
| **清除个数** | **12 / 12 = 100%** | ✅ 定向策略在真实模拟器全清 |
| 清除失败（空放）| 0 | ✅ 每次 clear 一次命中 |
| 虚拟时间 | 8 905 620 µs ≈ **8905.6 s** | 偏大，是主要优化对象 |
| 平均定位清除时间 | 8905.6 / 12 ≈ **742 s/个** | 评分指标，越低越好 |
| 程序墙钟时长 | **3.144 s** | ✅ 远小于 20 min 上限 |
| 检测指令(accepted) | 441 | 偏多 |
| 频道切换 | 340 | 偏多 |

**结论**：正确性已验证（9 个定向源全清、零空放），唯一短板是**效率**——虚拟时间 8905 s / 741 次动作，核心瓶颈在移动与冗余检测。

## 二、虚拟时间拆解（成本构成）

| 成分 | 计算 | 耗时(s) | 占比 |
|---|---|---|---|
| **移动** | 8905.6−2205−340−60 | **6300.6** | **70.7%** |
| 检测 | 441 × 5 s | 2205.0 | 24.8% |
| 切换 | 340 × 1 s | 340.0 | 3.8% |
| 清除 | 12 × 5 s（0 空放）| 60.0 | 0.7% |
| 合计 | | 8905.6 | 100% |

移动距离 = 6300.6 s × 5 m/s ≈ **31.5 km**。结论与问题 3 演练一致：**提速只能砍移动**，检测/切换是次要项。

## 三、代码级根因（`p3_robot.py`）

侦察点 31 个：`vantage_points()` = 原点 + 内环(k_ring=6, r=1000) + **外环(k_ring_outer=24, r=1780)**。
主循环 `run()` 的 L1（line 656-664）对每个点扫 `unanswered() + [仅 1 方位约束的频道]`：

1. **空频道被每个点重扫**（最大浪费）：8 个无源频道第一次返回 `no_signal` 后仍在 `unanswered()`，
   于是在全部 31 个点被反复重测 → 约 `8 × 31 ≈ 248` 次检测纯属浪费（占 441 的 **56%**）。
   这些重测不改变任何约束，只为"再确认一次无信号"。
2. **1 方位频道被每个后续点重测**：为凑第 2 方位把这类频道放进 `batch`，
   但**同点重测是共线、无效的**（固定偏差下结果不变）；真正有用的第 2 方位须来自不同位置，
   当前写法等于把所有后续点都用来反复补同一个频道。
3. **外环 24 点绕行**：24 点 @1780 m 的环周长 ~11 km，是 31.5 km 移动的主因；
   且 angular 顺序访问（不是按路程最优）进一步放大折返。

补充：L4 `uncovered_target()` 已经能基于"离所有扫描点 >1000 m 的覆盖缺口"补测找漏源，
所以 L1 对空频道的穷举重扫**与 L4 功能重复**，可大幅削减。

## 四、优化方向（按收益排序）

### P0 · 重构侦察（预期砍 30%~50% 虚拟时间，风险低）
- **空频道只扫 1~2 个分散点即标记，不再每点重扫**；最终完整性交给 L4 覆盖缺口补测。
  直接省掉 ~220 次检测（≈1100 s）+ 相应切换，且不增移动。
- **1 方位频道只在不同角向的点补测 1 次**（拿到非共线第 2 方位即停），不每个点都补。
- **外环从 24 点降到 12~16 点**（或自适应：仅在"内环 no_signal 但疑似边界源"的扇区补外点）。
  12 点环周长减半 → 移动与扫描同比例下降。注意：30 种子 mock 显示 12 点外环 = 0.9975，
  需以"不丢源"为验收前提（见第六节）。

### P1 · 路径/巡回优化（针对 70.7% 的移动）
- 把"内环/外环 angular 顺序"改成**最近邻 + 2-opt 巡回**：先把所有要停的点（侦察点 + 各频道逼近点）
  排成总路程最短的序，再顺路走；途中用已有的 `maybe_opportunistic_scan`（顺手扫）铺覆盖，零额外移动。
- 合并 L1 侦察与 L2/L3 定位清除为**单条巡回**，避免"先绕完 31 点再回来解"的两段式折返。

### P2 · 切换批处理（3.8%，顺手拿）
- 同点扫描频道按"保持测向机频道不变"的顺序分组；跨点时优先去与当前频道相同的目标，
  把 340 次切换再压一截（收益小但几乎零风险）。

### P3 · 保持零空放（不许回归）
- 当前 12 次 clear 全命中，说明 `near`/逼近逻辑到位。任何改动都要用"0 空放"当回归门槛。

## 五、预期效果（粗估）
P0+P1 同时落地、且保持 12/12：虚拟时间有望从 8905 s 降到 **4000~5000 s**，
平均定位清除时间 ~350~420 s/个；墙钟仍 <几秒，远在 20 min 内。

## 六、验证纪律（重要）
- 任何提速改动**必须先过 50 种子 mock（含定向）保持 ratio=1.000**，再过 2~3 次真实演练确认不丢源；
- 外环降到 12 点会引入 ~0.25% 漏清概率，正式测试前务必在演练里多跑几局确认稳定；
- 评分只关心"平均定位清除时间"这个数字（越低越好），没有隐藏时间上限（本局墙钟仅 3.1 s 已被接受）。

---
*附：`.jlog`/`.psum` 为加密封装（gzip + AES-256-GCM-chunked，RSA-OAEP 包装 DEK），
按模拟器设计参与者无法自解密；自评只能用 sqlite 明文汇总 + 代码静态分析，不能直接读动作序列。*
